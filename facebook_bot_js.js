let  btn = document.querySelectorAll("._517h") // put the classname of tab/element that is same in all other buttons/tags

btn.length //to check the total no of elemnts in page

btn.forEach(btn => btn.click()) //for each loop is used to re-run the program after clicking 
